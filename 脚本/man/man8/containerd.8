.TH CONTAINERD 8 "April 2026" "KUDIG-DATABASE 1.0" "System Administration"
.SH NAME
containerd \- 行业标准的容器运行时
.SH SYNOPSIS
.B containerd
[\fIOPTIONS\fR]
.SH DESCRIPTION
containerd 是一个行业标准的容器运行时，强调简单性、健壮性和可移植性。
它是 Docker 的核心组件，也是 Kubernetes CRI（容器运行时接口）的默认实现。
.PP
KUDIG-DATABASE 提供 containerd 深度解析，涵盖架构、配置、
镜像管理、容器生命周期等生产级内容。
.SH CORE FEATURES
.TP
.B 镜像管理
推送、拉取和存储容器镜像
.TP
.B 容器执行
创建、启动、停止容器进程
.TP
.B 存储管理
管理容器文件系统和镜像层
.TP
.B 运行时集成
支持 runc、crun、gVisor、Kata Containers
.TP
.B CRI 兼容
完整的 Kubernetes CRI 实现
.TP
.B 分布式
支持多租户和分布式系统
.SH ARCHITECTURE
.SS 核心组件
.TP
.B containerd
守护进程，管理容器生命周期
.TP
.B ctr
命令行客户端（开发和调试使用）
.TP
.B containerd-shim
容器进程管理器，解耦 containerd 和容器
.TP
.B runc
底层 OCI 运行时
.SS 命名空间
.TP
k8s.io
Kubernetes 使用的默认命名空间
.TP
moby
Docker 使用的命名空间
.TP
default
containerd 默认命名空间
.SH CTR COMMANDS
.SS 镜像操作
.RS
.nf
# 列出镜像
ctr -n k8s.io images list

# 拉取镜像
ctr images pull docker.io/library/nginx:latest

# 导入镜像
ctr images import image.tar

# 导出镜像
ctr images export image.tar nginx:latest
.fi
.RE
.SS 容器操作
.RS
.nf
# 列出容器
ctr -n k8s.io containers list

# 查看容器信息
ctr -n k8s.io containers info <container-id>

# 执行任务
ctr -n k8s.io tasks list

# 查看任务指标
ctr -n k8s.io tasks metrics <task-id>
.fi
.RE
.SH CONFIGURATION
配置文件路径：
.TP
.B /etc/containerd/config.toml
主配置文件
.PP
常用配置项：
.RS
.nf
[plugins."io.containerd.grpc.v1.cri"]
  sandbox_image = "registry.k8s.io/pause:3.9"

[plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc]
  runtime_type = "io.containerd.runc.v2"

[plugins."io.containerd.grpc.v1.cri".registry]
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors]
    [plugins."io.containerd.grpc.v1.cri".registry.mirrors."docker.io"]
      endpoint = ["https://mirror.example.com"]
.fi
.RE
.SH CRI IMPLEMENTATION
containerd 通过 cri 插件实现 Kubernetes CRI：
.TP
RuntimeService
管理 Pod 和容器生命周期
.TP
ImageService
管理镜像的拉取、查看和删除
.PP
Kubernetes 配置使用 containerd：
.RS
.nf
# kubelet 配置
--container-runtime=remote
--container-runtime-endpoint=unix:///run/containerd/containerd.sock
.fi
.RE
.SH SNAPSHOTTERS
.TP
overlayfs
默认快照器，使用 overlayfs 文件系统
.TP
native
简单快照器，完整复制层
.TP
zfs
使用 ZFS 文件系统
.TP
stargz
延迟拉取镜像层（eStargz）
.SH KUDIG DOCUMENTATION
.TP
CRI 深度解析
\fBdomain-3-control-plane/21-container-runtime-deep-dive.md\fR
.TP
容器镜像管理
\fBdomain-22-container-image-management/\fR
.TP
Docker 基础
\fBdomain-13-docker/\fR
.SH TROUBLESHOOTING
.TP
镜像拉取失败
检查 registry 配置、网络连通性、凭证
.TP
容器无法启动
检查运行时配置、资源限制、镜像完整性
.TP
CRI 连接失败
确认 socket 路径、containerd 运行状态
.TP
存储空间不足
清理未使用镜像、调整 GC 配置
.SH SEE ALSO
.BR ctr (1),
.Br runc (8),
.BR docker (8)
.PP
KUDIG-DATABASE: \fBdomain-3-control-plane/\fR, \fBdomain-13-docker/\fR
.SH AUTHOR
containerd Authors <https://containerd.io>
.SH COPYRIGHT
Apache License 2.0
