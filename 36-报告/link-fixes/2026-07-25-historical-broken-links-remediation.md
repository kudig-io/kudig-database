---
title: 历史断链治理报告
date: '2026-07-25'
type: link-fix-report
---

# 历史断链治理报告（2026-07-25）

> 承接 2026-07-25 散落文件归档工程后的存量断链专项治理。

## 治理范围与冻结区

- **活跃区**：全库 markdown（排除 `.git`/`.venv`/`33-源码` 等非文档目录）
- **冻结区**：`32-发布/package/2026-07-02_*` 三个历史发布快照包（11,233 个文件、约 70,400 处断链）——发布时刻的冻结产物，内部使用旧 `domain-XX` 路径体系自成一体，**不做改写**；同时其内部文件不作为重定向候选目标。

## 修复统计

| 批次 | 策略 | 修复量 |
|---|---|---|
| 批次1+2 | 旧 domain-XX→新目录映射 + 段级归一化路径亲和匹配 | 7,441 处（md 3,770 + wiki 3,671，涉及 836 文件） |
| 批次2补 | 剥离数字前缀回退索引（编号变更场景，如 `200-docker-*`→`01-docker-*`） | 151 处（md 93 + wiki 58，涉及 69 文件） |
| 批次3 | 目录型 wikilink `[[dir/]]` → `[[dir/index.md\|目录名]]` | 122 处（涉及 21 文件） |
| **合计** | | **7,714 处，涉及约 900 个文件** |

### 修复引擎关键规则

1. 旧顶级目录映射表：`domain-01-cluster-foundation`→`01-集群基础` 等 24 条（含 `concepts`→`22-概念`、`entities`→`23-实体`、无前缀中文旧名）
2. 段级归一化：剥离数字前缀后按路径后缀重合度打分，需文件名之外至少 1 段路径重合或同顶级目录，并列最高分视为不可判定（不修）
3. 通用文件名（README/index/CHANGELOG 等）不参与唯一同名重定向，仅在路径亲和度达标时修复
4. 冻结区文件不作为候选目标，防止活跃区链接误指快照包

## 残余断链（保留，供人工审阅）

活跃区残余约 **1,370 处**（wiki 335 + md 相对链接 1,035），明细见
[2026-07-25-residual-broken-links.json](./2026-07-25-residual-broken-links.json)。

| 类别 | 数量级 | 说明 |
|---|---|---|
| 真死链（目标文件已删除且无同名候选） | ~780 | 集中在 `37-归档`、`26-技能`、`27-标签` 等历史区 |
| `34-源码分析/` vendor 引用 | 134 | 源码 vendor 文件未入库，属预期缺失 |
| 歧义并列（多候选同分，不可自动判定） | ~450 | 如 `98-merged-indexes`（目录已删除）、多快照同名文件 |
| 模板占位符 `{{...}}` | ~20 | `31-脚本` 内生成模板，非真实链接，勿修 |

## 验证结论

- 修复后全量重扫**幂等收敛**（changed_files = 0）
- wiki 路径式链接有效数：26,580 → **30,430**（+3,850）
- md 相对链接有效数：2,921 → **6,784**（+3,863）
- 活跃区 wiki 断链：约 4,700 → **340**（降 93%）
- 无路径 `[[名称]]` 型 wikilink（10,970 处）由 Obsidian 按名称解析，不受影响
